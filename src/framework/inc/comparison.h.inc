	operator <(const a, b: typ): boolean;  cinline
	operator <=(const a, b: typ): boolean; cinline
	operator >(const a, b: typ): boolean;  cinline
	operator >=(const a, b: typ): boolean; cinline
	operator =(const a, b: typ): boolean; cinline
{$undef typ}